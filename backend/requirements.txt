# Direct production dependencies, loosely versioned — this is the file to
# edit when adding/upgrading a package. Docker and CI install from the
# fully pinned requirements.lock.txt / requirements-dev.lock.txt instead,
# for reproducible builds; regenerate those after changing this file (see
# the header comment in requirements.lock.txt for how).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
alembic>=1.13.0
gunicorn>=21.0
email-validator>=2.0.0
